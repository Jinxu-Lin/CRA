# Pilot: TRAK contrastive on toxicity filtering
# Experiment 0.5 -- 2x2 sanity check
_base_: "base.yaml"

attribution:
  method: "trak"
  scoring: "contrastive"

evaluation:
  n_seeds: 1
  task: "toxicity"
