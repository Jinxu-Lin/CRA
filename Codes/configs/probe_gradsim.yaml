# Probe: Grad-Sim on toxicity filtering
# Experiment 0 -- Critical Gate
_base_: "base.yaml"

attribution:
  method: "gradsim"

evaluation:
  n_seeds: 1
  task: "toxicity"
  metrics: ["lds", "auprc", "pk"]
